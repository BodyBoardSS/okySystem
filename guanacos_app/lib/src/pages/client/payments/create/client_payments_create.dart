import 'package:flutter/material.dart';

class ClientPaymentsCreate extends StatelessWidget {
  const ClientPaymentsCreate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Pagos'),
      ),
    );
  }
}