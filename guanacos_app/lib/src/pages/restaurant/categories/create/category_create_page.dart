import 'package:flutter/material.dart';

class CategoryCreatePage extends StatelessWidget {
  const CategoryCreatePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('CategoryCreatePage'),
    );
  }
}